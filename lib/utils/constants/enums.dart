enum TextSizes { small, medium, large }

enum MessageStatus { pending, sent, delivered, read, failed, deleted, edited }
enum FriendRelationship { none, pending, friend }
enum MessageType {image, video, link, document}